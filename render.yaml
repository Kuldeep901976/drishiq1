services:
  - type: web
    name: drishiq-preprod
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: preprod
      - key: NEXT_PUBLIC_APP_URL
        value: https://drishiq-preprod.onrender.com 